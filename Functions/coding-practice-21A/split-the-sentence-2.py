# Read the list of words from user input
word_list = input().split()

# Print each word on a separate line
for word in word_list:
    print(word)

#  The code starts by reading the list of words from user input.
#  The code then splits the list into individual words and prints each word on a separate line.
#  The code would print each word on a separate line.    